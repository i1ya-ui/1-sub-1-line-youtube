# AI Log

Журнал изменений, внесённых ИИ.

## +2 подписчика
Добавлен `useState` и счётчик подписчиков `subs` (начальное значение 2).

## +6 подписчиков (2025-03-14)
- Добавлены кнопки реакций 🔥 и рандомный кубик 🎲

## +15 подписчиков (2025-03-14)
- Лидерборд зрителей, чат, виральное шеринг, тиры разблокировки, цикл настроений, серия кликов

## +11 подписчиков (2026-03-15)
- Включён хаос-режим с мем-кнопкой, баннером и хаос-очками для зрителей

## +5 подписчиков (2026-03-15)
- Добавлен хайп-трекер с кнопкой буста для чата

## +1 подписчик (2026-03-18)
- Фон и масштабы стрима теперь сходят с ума от хайпа и хаоса

## +6 подписчиков (2026-03-18)
- Добавлена паник-кнопка, которая врубает хаос и резко бустит хайп на стриме

## +1 подписчик (2026-03-18)
- Появляется скрытая надпись «Секрет» после первого нажатия паник-кнопки

## +20 подписчиков (2026-03-20)
- **Добавлено:** аватар профиля, лайки, лента постов, раздел профилей, эмодзи-бар (🔥👍😂💀), колокольчик уведомлений, кнопка «Подписаться», подписка на случайного, поле комментариев, таймстампы в чате, переключатель темы (светлая/тёмная), бейдж #viral, пасхалка (Konami-код)
- **Изменено:** заголовок → «1 Sub 1 Line — Соцсеть», фон → градиент, лента реакций расширена, лидерборд — топ-5 с аватарами, шапка крупнее и светится при хайпе
- **Сломано/удалено:** паник-кнопка и мем-кнопка — убраны (хаос-режим)

## +20 подписчиков (2026-03-20)
- **Добавлено:** стикеры (плавающие emoji), сторис-бар, репост, ЛС (личные сообщения), LIVE-бейдж, голосование A/B под постами, галочка ✓, три темы (тёмная/светлая/матрица), тренды (#viral, #стрим...), монетки, RAID-кнопка, заглушить, GIF-кнопка (заглушка), конфетти при лайке
- **Изменено:** посты — с автором и датой, чат — пузырьки, профили — карточки с био, заголовок мигает при >3 уведомлениях
- **Сломано/удалено:** кубик и лидерборд — убраны (хаос)

## +23 подписчика (2026-03-23)
- **Добавлено:** ESLint (правила React, es2022), Prettier (semi: false, singleQuote), API-клиент (`src/api/client.js`) с методами get/post/del, npm-скрипты lint и format
- **Улучшено:** Code quality — статический анализ и форматирование, основа для будущего REST API
- **Компромиссы:** API-клиент пока не используется в App (ждёт бэкенд), нет настроек ESLint для JSX-атрибутов
- **Дальше:** Подключить API к постам/чату, добавить backend (Node/PostgreSQL), миграции

## +23 подписчика (2026-03-23)
- **Добавлено:** UI-auth фундамент: `user`, `login/logout`, индикатор `@username`, кнопка входа/выхода
- **Улучшено:** чат и ЛС теперь доступны только после входа, поле комментария блокируется для гостя
- **Компромиссы:** auth пока локальный (без JWT/сессий и backend), нет формы signup
- **Дальше:** вынести auth в отдельный модуль, подключить API `/auth/login` и `/auth/signup`

## +21 подписчик (2026-03-24)
- **Добавлено:** `src/auth/session.js` — `loadUser` / `saveUser` в `localStorage`; вход/выход пишет сессию; `subs = 44`; подпись в шапке про localStorage; `useEffect` — заголовок вкладки `1 Sub — @name` при входе
- **Улучшено:** сессия переживает перезагрузку; вкладка браузера показывает ник
- **Компромиссы:** только клиент, без JWT и сервера
- **Дальше:** JWT, `/me`, инвалидация при 401

## +22 подписчика (2026-03-27)
- **Добавлено:** client-side валидация ника при входе (`^[a-zA-Z0-9_]{3,20}$`) и UI-ошибка под формой.
- **Улучшено:** auth UX стал предсказуемее: пользователь сразу видит причину отказа и может исправить ник.
- **Компромиссы:** правило валидации пока только на клиенте; нет проверки занятости ника на сервере.
- **Дальше:** вынести схему валидации в общий модуль и дублировать её в backend API.

## +150 бонусных строк (14 дней)
- **Добавлено:** backend auth `server/index.js` (Express + JWT): `/api/auth/signup`, `/api/auth/login`, `/api/auth/me`; Dockerfile для API: `server/Dockerfile`; proxy в `vite.config.js`; фронт подключён к backend (`src/api/client.js`, `src/auth/session.js`, `src/App.jsx`)
- **Улучшено:** авторизация теперь реальная (клиент+сервер), сессия валидируется через `/auth/me`, при 401 выполняется logout
- **Компромиссы:** users пока in-memory (без PostgreSQL и миграций), пароль без хеша в MVP
- **Дальше:** подключить PostgreSQL + миграции, bcrypt, refresh token, rate limit, endpoint для профиля

## Рефактор структуры (frontend/backend)
- **Добавлено:** новая структура верхнего уровня: `frontend/*` и `backend/*`
- **Перенесено:** клиент (`index.html`, `vite.config.js`, `src/*`, Dockerfile) в `frontend`; API (`index.js`, Dockerfile) в `backend`
- **Изменено:** корневые npm-скрипты теперь запускают `frontend` и `backend` по новым путям
- **Результат:** фронт и бэк физически разделены, проще масштабировать и деплоить отдельно

## Разделение зависимостей (frontend/backend)
- **Добавлено:** отдельные `frontend/package.json` и `backend/package.json`
- **Изменено:** корневой `package.json` стал оркестратором через `npm --prefix ...`
- **Исправлено:** `frontend/vite.config.js` без `root`; Dockerfile'ы используют локальные `package*.json`
- **Результат:** frontend/backend теперь независимы по зависимостям и сборке

## Удаление root package
- **Удалено:** корневые `package.json`, `package-lock.json`, `.eslintrc.cjs`, `.prettierrc`
- **Изменено:** запуск только из `frontend` и `backend` без общего оркестратора
- **Обновлено:** `README.md` под автономные команды
- **Результат:** полностью раздельные приложения по пакетам и конфигам

## Backend -> TypeScript
- **Добавлено:** `backend/src/index.ts` и `backend/tsconfig.json`
- **Изменено:** backend-скрипты на TS-цикл (`build` -> `dist`, `start` из `dist`)
- **Обновлено:** `backend/Dockerfile` теперь собирает TS в build stage и запускает `dist/index.js`
- **Удалено:** `backend/index.js` (JS-версия)

## Railway config for split services
- **Добавлено:** `backend/railway.toml` и `frontend/railway.toml` с явными build/start командами
- **Обновлено:** `README.md` с инструкцией деплоя двух сервисов (`Root Directory`: `backend` и `frontend`)
- **Улучшено:** Railpack больше не пытается собирать монорепу из корня без `package.json`

## Frontend TS + Backend PostgreSQL
- **Добавлено:** полный переход фронта на TypeScript (`App.tsx`, `main.tsx`, `client.ts`, `session.ts`, `types.ts`, `vite.config.ts`, `tsconfig.json`)
- **Изменено:** frontend ESLint и scripts теперь поддерживают `ts/tsx`
- **Добавлено (backend):** PostgreSQL через `DATABASE_URL`, автоинициализация таблицы `users`, регистрация/логин теперь читают/пишут БД
- **Безопасность:** пароли больше не в plain text, используется хеширование (scrypt + salt)

## +21 подписчик (2026-03-28)
- **Добавлено:** таблица `posts` в PostgreSQL; `GET /api/posts` (лента до 50 записей с автором и датой); `POST /api/posts` (JWT, поле `body`); на фронте загрузка ленты с API при монтировании; публикация поста через API с последующим рефетчем; счётчик `subs` → 76.
- **Улучшено:** лента и новые посты переживают перезагрузку и общие между клиентами; лайки по-прежнему только локально до появления колонки `likes` в БД.
- **Компромиссы из-за лимита строк:** сжатый SQL и однострочные хендлеры/функции на фронте; нет пагинации, валидации длины текста и миграций отдельно от `initDb`; лайки не синхронизируются с сервером.
- **Дальше:** миграции (или `ALTER` для `likes`), `PATCH` лайков, индексы и лимиты тела поста, постепенно разгрузить «компактный» код обратно в читаемый вид.

## +8 подписчиков (2026-03-29)
- **Добавлено:** колонка `likes` в PostgreSQL (`ALTER ... IF NOT EXISTS`), отдача реальных лайков в `GET /api/posts`, лимит тела поста `MAX_POST_BODY = 2000` с ответом `Too long`, `PATCH /api/posts/:id/like` (инкремент, JWT), клиентский `patch()`, на фронте лайк для вошедших пользователей уходит на API с рефетчем ленты; гости по-прежнему только локальный оптимистичный счётчик; `subs` → 84.
- **Улучшено:** лайки переживают перезагрузку и синхронизируются между клиентами для авторизованных действий; сервер отсекает слишком длинные посты.
- **Компромиссы:** нет таблицы «кто лайкнул» (один пользователь может накручивать), при ошибке API остаётся оптимистичное число без отката, `ALTER` в `initDb` вместо отдельных миграций.
- **Дальше:** уникальность лайка на пару (user_id, post_id), `GET` с пагинацией, дублирование лимита длины на фронте для UX.

## +1 подписчик (2026-03-29)
- **Добавлено:** `frontend/src/constants.ts` с `MAX_POST_BODY = 2000` (в ровень с бэкендом); у поля поста `maxLength`, счётчик `длина/лимит`, ранняя проверка перед `POST`, кнопка «Опубликовать» отключена при превышении лимита; `subs` → 64.
- **Улучшено:** пользователь видит лимит до отправки; лишний запрос при слишком длинном тексте не уходит на сервер.
- **Компромиссы:** константа продублирована числом в двух пакетах (при смене лимита нужно править оба места или вынести в общий пакет).
- **Дальше:** общий пакет `shared/` или генерация констант из одного источника; уникальные лайки в БД.

## +1 подписчик (2026-03-30)
- **Добавлено:** состояние `postError`, `try/catch` вокруг публикации поста, вывод текста ошибки API под счётчиком символов; `subs` → 65.
- **Улучшено:** при сетевой ошибке или ответе 4xx/5xx пользователь видит сообщение вместо «тихого» провала.
- **Компромиссы:** одна общая строка ошибки без кода статуса и без retry.
- **Дальше:** индикатор загрузки при отправке поста, очистка ошибки при изменении текста.

## +1 подписчик (2026-03-30)
- **Добавлено:** `GET /api/health` (`{ ok: true }`); `GET /api/users/:name` — публичный профиль по нику (`id`, `name`); на фронте состояние `posting`, `finally` сбрасывает загрузку, `useEffect` очищает `postError` при смене текста поста; поле и кнопка с `readOnly`/`disabled` и `aria-busy` во время отправки, подпись кнопки «...»; `subs` → 66.
- **Улучшено:** деплой/мониторинг могут пинговать health; клиенты могут подтянуть профиль по API; публикация не дублируется двойным кликом, ошибка не «залипает» при правке текста.
- **Компромиссы:** профиль без `bio`/аватара в БД — только `id` и `name`; health без проверки БД.
- **Дальше:** колонки профиля и `PATCH /api/users/me`, связать карточки «Профили» с `GET /users/:name`.

## +4 подписчика (2026-04-01)
- **Добавлено:** `GET /api/health` с `SELECT 1` и `{ ok, db }` / `503`; `express.json({ limit: '256kb' })`; `NAME_RE` и проверки ника при signup/login; лимит пароля 128 на signup; индекс `idx_posts_created_at`; `app.disable('x-powered-by')`; middleware с `X-Content-Type-Options` и `X-Frame-Options`; `POST /api/posts` возвращает `id` через `RETURNING`; на фронте рефетч ленты при `visibilitychange` (возврат на вкладку).
- **Улучшено:** health отражает БД; меньше риска JSON DoS и clickjacking; согласованная валидация ника с клиентским UX; лента обновляется при переключении вкладок без ручного обновления страницы.
- **Компромиссы:** лимит строк — без rate limit, без отдельного `readiness`, индекс добавлен через `initDb`, а не миграцию; строка `subs` в `App.tsx` не менялась по просьбе автора.
- **Дальше:** миграции (Flyway/node-pg-migrate), уникальные лайки (`post_likes`), комментарии, связать профили UI с `GET /api/users/:name`.

## +2 подписчика (2026-04-01)
- **Добавлено:** `GET /api/health` с `SELECT 1` и `{ ok, db }` / `503`; `NAME_RE`, проверка ника на signup/login, лимит пароля 128 на signup; `express.json({ limit: '256kb' })`; `app.disable('x-powered-by')`; middleware `nosniff` + `X-Frame-Options: DENY`; индекс `idx_posts_created_at`; `POST /api/posts` возвращает `id`; на фронте публикация по **Ctrl+Enter / ⌘+Enter** и подсказка в `title`.
- **Улучшено:** health отражает БД; базовая защита от больших JSON и clickjacking; сортировка ленты по индексу; быстрее публиковать с клавиатуры.
- **Компромиссы:** без rate limit и отдельных миграций; строка `subs` не менялась.
- **Добавлено:** `postsLoading` и первичная загрузка ленты с `finally`; рефетч ленты при `visibilitychange` (возврат на вкладку); текст «Загрузка ленты…»; `autoComplete` для логина/пароля (`new-password` при signup).
- **Улучшено:** видно состояние загрузки; лента подтягивается после переключения вкладок; менеджеры паролей корректнее подставляют поля.
- **Компромиссы:** лимит 20 строк — без скелетона постов и без индикатора ошибки при падении `GET /posts`.
- **Дальше:** уникальные лайки в БД, комментарии к постам, миграции. показ ошибки загрузки ленты и кнопка «Повторить».

## +1 подписчика (2026-04-03)
- **Добавлено:** `postsFetchError` и `fetchFeed` (`useCallback`): единая загрузка ленты при монтировании, по кнопке «Повторить» и при возврате на вкладку; сообщение при сбое `GET /posts`; удалён дублирующий второй обработчик `visibilitychange`; `fetchFeed({ silent: true })` при `visibilitychange` — без полноэкранного спиннера и без сброса текста ошибки; ручное «Обновить» в шапке ленты; `role="alert"` у блока ошибки ленты; **backend:** таблица `post_likes` с PK `(user_id, post_id)`, индекс `idx_post_likes_post_id`; `PATCH /api/posts/:id/like` с `ON CONFLICT DO NOTHING`, счётчик в `posts` только при новой строке, ответ `{ ok, liked }`; при `liked: false` или сетевой ошибке откатывается оптимистичный лайк; `subs` → 72.
- **Улучшено:** ошибка ленты видна, запрос можно повторить без перезагрузки; один пользователь не накручивает лайк поста через API; фоновое обновление ленты не мигает «Загрузка ленты…».
- **Компромиссы:** одна текстовая ошибка без деталей ответа; счётчик `posts.likes` для старых накруток до миграции не пересчитывается; нет `liked` в `GET /posts` (UI не знает заранее, что уже лайкнуто).
- **Дальше:** `GET /posts` с флагом «лайкнуто мной» при JWT; комментарии; отдельные миграции вместо только `initDb`.

## +9 подписчиков (2026-04-03)
- **Добавлено:** `GET /api/posts` при необязательном `Authorization` подставляет `liked` через `EXISTS` по `post_likes`; фронт передаёт токен в `get('/posts')` и после поста/лайка рефетчит с токеном; тип поста с `liked?`; для **вошедших** кнопка лайка отключена, если пост уже лайкнут (`❤️` / `🤍`); гости могут кликать (локальный счётчик); оптимистично `liked: true` только с JWT; `subs` → 80.
- **Улучшено:** после перезагрузки видно, какие посты уже лайкнуты текущим пользователем; гости по-прежнему локально крутят только счётчик.
- **Компромиссы:** лимит строк — без отдельного optional-auth middleware и без `aria`-подписи к состоянию лайка; невалидный токен обрабатывается как гость.
- **Дальше:** комментарии в БД и API; миграции; при желании — unlike через `DELETE` из `post_likes`.

## +2 подписчика (2026-04-03)
- **Добавлено:** таблица `comments`, индекс по посту; `MAX_COMMENT_BODY` (500) на бэке и во фронте; `GET /api/posts` отдаёт до 12 комментариев на пост (`LATERAL` + `json_agg`); `POST /api/posts/:id/comments` (JWT, проверка поста); под постом список `@автор текст`, поле и «Ок»; `subs` → 82.
- **Улучшено:** комментарии переживают перезагрузку и общие между клиентами.
- **Компромиссы:** лимит строк — без ошибки UI при сбое комментария, без пагинации комментариев и без миграций отдельно от `initDb`.
- **Дальше:** счётчик комментариев в ленте, удаление/редакция, уведомления.

## +4 подписчика (2026-04-03)
- **Добавлено:** в `GET /api/posts` поле `commentCount` (подзапрос `COUNT` по `comments`); в карточке поста `💬` и число; при отправке комментария — `cPosting`, `cErr`, счётчик символов; у поля комментария подсказка `title` и отправка по **Enter**; при ошибке API — кнопка «Повторить»; `subs` → 86.
- **Улучшено:** видно полное число комментариев, даже если в ответе только последние 12; индикатор «...» при отправке; быстрее отвечать с клавиатуры; не нужно заново набирать текст после сбоя.
- **Компромиссы:** Enter не добавляет перевод строки (однострочное поле); повтор использует тот же текст из поля; тип `PostItem` разнесён на несколько строк ради читаемости.
- **Дальше:** многострочные комментарии (`textarea`), пагинация комментариев, удаление своих комментариев.

## +1 подписчик (2026-04-03)
- **Добавлено:** `GET /api/posts` — `commentCount` и в JSON комментария поле `userId`; `DELETE /api/posts/:postId/comments/:id` (только свой `user_id`); клиент `del()`; в карточке — `💬` с числом и кнопка «Удалить» у своих комментариев; `subs` → **83**.
- **Улучшено:** видно полное число комментариев; автор может убрать свой ответ без правки БД вручную.
- **Компромиссы:** без подтверждения удаления и без optimistic UI; ошибка DELETE не показывается.
- **Дальше:** confirm перед удалением, пагинация комментариев.

## +4 подписчика (2026-04-03)
- **Добавлено:** комментарии через **`textarea`** (несколько строк, `resize: vertical`), отправка по **Ctrl+Enter / ⌘+Enter**; перед удалением — **`confirm`**; при ошибке `DELETE` — текст в `delErr`; запасной показ «Удалить» по **`user?.name === c.author`**, если нет `userId`; тема (**Тёмная / Светлая / Матрица**) сохраняется в **`localStorage`** (`1sub1line_theme`); у каждого поста **`id="post-{id}"`**, кнопка **«Ссылка»** копирует URL с хэшем **`#post-{id}`** (2 с подсказкой «Скопировано»); при открытии страницы с хэшем — **плавная прокрутка** к посту; `subs` → **91**.
- **Улучшено:** многострочный текст в ответах; случайное удаление реже; видна ошибка сети/API при удалении; можно делиться постом; тема не сбрасывается при перезагрузке.
- **Компромиссы:** системный `confirm` вместо модала; без отдельной страницы поста; `clipboard` может требовать HTTPS/разрешений.
- **Дальше:** пагинация комментариев, собственный модал вместо `confirm`, toast вместо смены текста кнопки, deep-link если появятся роуты.

## +1 подписчик (2026-04-03 · toast, темы, очередь, reduced motion)
- **Добавлено:** **toast** после копирования ссылки (`role="status"`, `aria-live="polite"`, плашка снизу, ~2.5 с; при reduce motion — 2 с и подписка на смену системной настройки); при сбое буфера — «Не удалось скопировать»; кнопка «Ссылка» с фиксированной подписью; стили плашки по теме (светлая — белая с тёмным текстом, матрица — зелёный акцент и рамка, тёмная — как в первой итерации); **очередь toast** — быстрые клики не затирают сообщения; **deep-link** — `scrollIntoView` без плавности при reduce motion; `subs` → **92**.
- **Улучшено:** заметная обратная связь, читаемость на любом фоне, предсказуемость уведомлений и уважение к `prefers-reduced-motion`.
- **Компромиссы:** без лимита длины очереди; без анимации появления/исчезновения toast.
- **Дальше:** кап очереди; лёгкая анимация opacity при отсутствии reduce motion.

## +4 подписчика (2026-04-13 · био, публичный профиль, блок «Профили»)
- **Добавлено:** колонка `users.bio` в `initDb`; `GET /api/auth/me` и `GET /api/users/:name` отдают `bio`; `PATCH /api/users/me` обновляет био с проверкой длины; `export const MAX_BIO = 280` в `frontend/src/constants.ts` — лимит и счётчик на клиенте; по токену — поле «О себе», сохранение через API; при ошибке `PATCH` — toast «Не удалось сохранить био»; `PROFILE_SEED` (ники + запасной текст); при монтировании — параллельные `GET /users/:name` для карточек «Профили» (`id`/`name`/`bio` с сервера, эмодзи из seed); при пустом `bio` в БД — `u.bio || p.bio`; при 404/сети — исходный seed; `subs` → **96** (дальнейшие доработки того же дня — без прироста подписчиков).
- **Улучшено:** «О себе» в PostgreSQL и после перезагрузки; один источник лимита на клиенте; публичный API и блок «Профили» согласованы с БД; виден сбой сохранения био.
- **Компромиссы:** сохранение био может быть зашито в длинный `onClick`; нет текста ошибки с кодом ответа; в «Профилях» фиксированный список ников; после «Сохранить био» карточки не рефетчатся; один `useEffect` без отмены запроса.
- **Дальше:** после успешного `PATCH /users/me` обновлять карточку с тем же ником; список профилей из уникальных авторов ленты; при желании — код ответа в UI, ETag/валидация на клиенте.

## +0 подписчиков (2026-04-13 · био: UX и синхронизация)
- После `PATCH /users/me` карточка «Профили» обновляется сразу; добавлен success toast «Био сохранено».
- `bioSaved`/`bioDirty`: `PATCH` только при реальном diff, кнопка disabled без изменений.
- Быстрое редактирование: **Ctrl+Enter / ⌘+Enter** сохранить, **Esc** откат; подсветка черновика, «Отменить», статус несохранённых изменений.

## +0 подписчиков (2026-04-13 · просмотр профилей пользователей)
- Добавлен выбор профиля по клику (`activeProfile`): highlight карточки и отдельный блок с `@name`, `id`, `bio`.
- Появился явный режим просмотра пользователя без роутинга.
- Пока работает только в рамках текущего `profiles` (seed + подгрузка), без отдельной страницы/URL.
- Дальше: deep-link `#profile-{name}` и кнопка «Открыть профиль» из поста/комментария.
