# AI Log

Журнал изменений, внесённых ИИ.

## +2 подписчика
Добавлен `useState` и счётчик подписчиков `subs` (начальное значение 2).

## +6 подписчиков (2025-03-14)
- Добавлены кнопки реакций 🔥 и рандомный кубик 🎲

## +15 подписчиков (2025-03-14)
- Лидерборд зрителей, чат, виральное шеринг, тиры разблокировки, цикл настроений, серия кликов

## +11 подписчиков (2026-03-15)
- Включён хаос-режим с мем-кнопкой, баннером и хаос-очками для зрителей

## +5 подписчиков (2026-03-15)
- Добавлен хайп-трекер с кнопкой буста для чата

## +1 подписчик (2026-03-18)
- Фон и масштабы стрима теперь сходят с ума от хайпа и хаоса

## +6 подписчиков (2026-03-18)
- Добавлена паник-кнопка, которая врубает хаос и резко бустит хайп на стриме

## +1 подписчик (2026-03-18)
- Появляется скрытая надпись «Секрет» после первого нажатия паник-кнопки

## +20 подписчиков (2026-03-20)
- **Добавлено:** аватар профиля, лайки, лента постов, раздел профилей, эмодзи-бар (🔥👍😂💀), колокольчик уведомлений, кнопка «Подписаться», подписка на случайного, поле комментариев, таймстампы в чате, переключатель темы (светлая/тёмная), бейдж #viral, пасхалка (Konami-код)
- **Изменено:** заголовок → «1 Sub 1 Line — Соцсеть», фон → градиент, лента реакций расширена, лидерборд — топ-5 с аватарами, шапка крупнее и светится при хайпе
- **Сломано/удалено:** паник-кнопка и мем-кнопка — убраны (хаос-режим)

## +20 подписчиков (2026-03-20, волна 2)
- **Добавлено:** стикеры (плавающие emoji), сторис-бар, репост, ЛС (личные сообщения), LIVE-бейдж, голосование A/B под постами, галочка ✓, три темы (тёмная/светлая/матрица), тренды (#viral, #стрим...), монетки, RAID-кнопка, заглушить, GIF-кнопка (заглушка), конфетти при лайке
- **Изменено:** посты — с автором и датой, чат — пузырьки, профили — карточки с био, заголовок мигает при >3 уведомлениях
- **Сломано/удалено:** кубик и лидерборд — убраны (хаос)

## +23 подписчика (2026-03-23)
- **Добавлено:** ESLint (правила React, es2022), Prettier (semi: false, singleQuote), API-клиент (`src/api/client.js`) с методами get/post/del, npm-скрипты lint и format
- **Улучшено:** Code quality — статический анализ и форматирование, основа для будущего REST API
- **Компромиссы:** API-клиент пока не используется в App (ждёт бэкенд), нет настроек ESLint для JSX-атрибутов
- **Дальше:** Подключить API к постам/чату, добавить backend (Node/PostgreSQL), миграции
